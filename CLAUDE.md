# CLAUDE.md

本项目的 agent 执行约定以 **`AGENTS.md`** 为唯一正本。

本文件不重复内容，只做指针，避免两份文档漂移打架。

- 执行约定 / 模块边界 / 验证矩阵 / 安全审计 → 见 `AGENTS.md`
- 本版范围与完成线 → 见项目内 `scope/` 下当前版本的 `v*-done.md` 文件，例如 `scope/v1-done.md`（每版重写、冻结）

开工前先读这两份。
